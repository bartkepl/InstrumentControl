<UserControl x:Class="InstrumentControl.App.Views.DataViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             Background="{StaticResource BackgroundBrush}">

    <DockPanel>
        <!-- Toolbar -->
        <Border DockPanel.Dock="Top" Background="White" Padding="8,6">
            <StackPanel Orientation="Horizontal">
                <Button Content="🗑 Wyczyść" Style="{StaticResource DangerButtonStyle}"
                        Command="{Binding ClearDataCommand}" Margin="2,0"/>
                <Button Content="📥 Eksport CSV" Style="{StaticResource AccentButtonStyle}"
                        Command="{Binding ExportCsvCommand}" Margin="2,0"/>
                <Separator Margin="4,0"/>
                <TextBlock VerticalAlignment="Center" FontSize="12" Foreground="#7F8C8D">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Wyniki: {0}">
                            <Binding Path="ResultCount"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Split: table top, chart bottom -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="80"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="*" MinHeight="80"/>
            </Grid.RowDefinitions>

            <!-- Data grid -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="8,8,8,0">
                <DataGrid ItemsSource="{Binding Results}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          AlternatingRowBackground="#F8F9FA"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeight="24" FontSize="12"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Czas" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="90"/>
                        <DataGridTextColumn Header="Przyrząd" Binding="{Binding InstrumentName}" Width="130"/>
                        <DataGridTextColumn Header="Kanał" Binding="{Binding ChannelId}" Width="60"/>
                        <DataGridTextColumn Header="Parametr" Binding="{Binding ParameterName}" Width="130"/>
                        <DataGridTextColumn Header="Wartość" Binding="{Binding Value, StringFormat=G8}" Width="100"/>
                        <DataGridTextColumn Header="Jednostka" Binding="{Binding Unit}" Width="70"/>
                        <DataGridTextColumn Header="Funkcja" Binding="{Binding Function}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <GridSplitter Grid.Row="1" Height="4" HorizontalAlignment="Stretch" Background="#BDC3C7"/>

            <!-- Chart -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="8,0,8,8">
                <oxy:PlotView Model="{Binding PlotModel}" Background="White"/>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
